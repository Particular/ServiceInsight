<Window x:Class="ServiceInsight.Shell.ManageLicenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:ServiceInsight.Controls"
        xmlns:shell="clr-namespace:ServiceInsight.Shell"
        mc:Ignorable="d" 
        d:DesignHeight="340" d:DesignWidth="500" 
        MinHeight="340" MinWidth="500"
        ShowInTaskbar="False" 
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource DialogWindowStyle}" d:DataContext="{d:DesignInstance shell:ManageLicenseViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock Text="LICENSE INFORMATION" Margin="20,10,10,10" FontSize="14"  FontWeight="Bold" />
            <Rectangle Fill="{StaticResource WindowBorderBrush}" Height="1" Margin="-8" />
        </StackPanel>

        <Grid Grid.Row="1" VerticalAlignment="Top">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Margin="20,20,20,0" Grid.Row="0">
                <Run Text="Platform license type:" FontWeight="Bold" FontSize="14" />
                <Run Text="{Binding LicenseType}" />
            </TextBlock>

            <TextBlock Margin="20,20,20,0" Grid.Row="1" Visibility="{Binding ExpirationDate, Converter={StaticResource NullToVisibilityConverter}}">
                <Run Text="Platform license expiry date:" FontWeight="Bold" FontSize="14" />
                <Run Text="{Binding ExpirationDate, StringFormat=d}" />
            </TextBlock>

            <TextBlock Margin="20,20,20,0" Grid.Row="2" Visibility="{Binding UpgradeProtectionExpirationDate, Converter={StaticResource NullToVisibilityConverter}}">
                <Run Text="Upgrade protection expiry date:" FontWeight="Bold" FontSize="14" />
                <Run Text="{Binding UpgradeProtectionExpirationDate, StringFormat=d}" />
            </TextBlock>

            <Button x:Name="ImportLicense" 
                    HorizontalAlignment="Left"
                    Grid.Row="3" 
                    Margin="20,20,20,20"
                    Content="Import license file..." 
                    Width="140" Height="30"
                    Style="{StaticResource CallToActionButton}"/>

        </Grid>

        <Rectangle Fill="{StaticResource WindowBorderBrush}" Height="1" Margin="-8" Grid.Row="2" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="20,10,20,10">
            <TextBlock Text="Need help?" Margin="2" />
            <controls:LinkedIcon Text="Contact support" />
        </StackPanel>

    </Grid>
</Window>







