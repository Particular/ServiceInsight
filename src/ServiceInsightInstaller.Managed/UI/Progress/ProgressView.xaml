<UserControl x:Class="ServiceInsightInstaller.Managed.UI.Progress.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:progress="clr-namespace:ServiceInsightInstaller.Managed.UI.Progress"
             d:DataContext="{d:DesignInstance progress:ProgressViewModel}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <DockPanel>

        <StackPanel>
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Message}" />

            <ProgressBar Maximum="100" Value="{Binding OverallProgress, Mode=OneWay}" />
        </StackPanel>

    </DockPanel>
</UserControl>