<UserControl x:Class="NServiceBus.Profiler.Desktop.Saga.SagaWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Images/Xaml/XamlIcons.xaml" />
                <ResourceDictionary>

                    <SolidColorBrush x:Key="SagaBackground" Color="#F2F2F2" />
                    <SolidColorBrush x:Key="SagaForeground" Color="#808080" />
                    <SolidColorBrush x:Key="SagaBlue" Color="#307BC6" />
                    <SolidColorBrush x:Key="FooterBackground" Color="#333333" />
                    <SolidColorBrush x:Key="MessageBackground" Color="#CCCCCC" />

                    <ControlTemplate x:Key="ValidationToolTipTemplate">
                        <Grid x:Name="Root" Margin="5,0" RenderTransformOrigin="0,0" Opacity="0">
                            <Grid.RenderTransform>
                                <TranslateTransform x:Name="xform" X="-25"/>
                            </Grid.RenderTransform>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup Name="OpenStates">
                                    <VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="0"/>
                                        <VisualTransition To="Open" GeneratedDuration="0:0:0.2">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="xform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <BackEase Amplitude=".3" EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </VisualTransition>
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="Closed">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" To="0" Duration="0"/>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Open">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="xform" Storyboard.TargetProperty="X" To="0" Duration="0"/>
                                            <DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" To="1" Duration="0"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Border Margin="4,4,-4,-4" Background="#052A2E31" CornerRadius="5"/>
                            <Border Margin="3,3,-3,-3" Background="#152A2E31" CornerRadius="4"/>
                            <Border Margin="2,2,-2,-2" Background="#252A2E31" CornerRadius="3"/>
                            <Border Margin="1,1,-1,-1" Background="#352A2E31" CornerRadius="2"/>

                            <Border Background="#FFDC000C" CornerRadius="2"/>
                            <Border CornerRadius="2">
                                <TextBlock 
                  UseLayoutRounding="false" 
                  Foreground="White" Margin="8,4,8,4" MaxWidth="250" TextWrapping="Wrap" Text="{Binding (Validation.Errors)[0].ErrorContent}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>


                    <Style TargetType="RadioButton" x:Key="PropertiesRadioButton">
                        <Setter Property="Background" Value="#FF448DCA"/>
                        <Setter Property="Foreground" Value="{StaticResource SagaBlue}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Top"/>
                        <Setter Property="Padding" Value="4,1,0,0"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush">
                            <Setter.Value>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FFA3AEB9" Offset="0"/>
                                    <GradientStop Color="#FF8399A9" Offset="0.375"/>
                                    <GradientStop Color="#FF718597" Offset="0.375"/>
                                    <GradientStop Color="#FF617584" Offset="1"/>
                                </LinearGradientBrush>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="RadioButton">
                                    <StackPanel>
                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CommonStates">
                                                <VisualState x:Name="Normal"/>
                                                <VisualState x:Name="MouseOver">
                                                </VisualState>
                                                <VisualState x:Name="Pressed">
                                                    <Storyboard>

                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Disabled">
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="contentPresenter" Storyboard.TargetProperty="(UIElement.Opacity)" Duration="0" To=".55"/>
                                                    </Storyboard>
                                                </VisualState>
                                            </VisualStateGroup>
                                            <VisualStateGroup x:Name="CheckStates">
                                                <VisualState x:Name="Checked">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="contentPresenter" Storyboard.TargetProperty="FontWeight">
                                                            <DiscreteObjectKeyFrame KeyTime="0" >
                                                                <DiscreteObjectKeyFrame.Value>
                                                                    <FontWeight>Bold</FontWeight>
                                                                </DiscreteObjectKeyFrame.Value>
                                                            </DiscreteObjectKeyFrame>
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Unchecked"/>
                                            </VisualStateGroup>
                                            <VisualStateGroup x:Name="FocusStates">
                                                <VisualState x:Name="Focused">
                                                    <Storyboard>
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="Unfocused" />
                                            </VisualStateGroup>
                                            <VisualStateGroup x:Name="ValidationStates">
                                                <VisualState x:Name="Valid"/>
                                                <VisualState x:Name="InvalidUnfocused">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ValidationErrorElement" Storyboard.TargetProperty="Visibility">
                                                            <DiscreteObjectKeyFrame KeyTime="0" >
                                                                <DiscreteObjectKeyFrame.Value>
                                                                    <Visibility>Visible</Visibility>
                                                                </DiscreteObjectKeyFrame.Value>
                                                            </DiscreteObjectKeyFrame>
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                                <VisualState x:Name="InvalidFocused">
                                                    <Storyboard>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ValidationErrorElement" Storyboard.TargetProperty="Visibility">
                                                            <DiscreteObjectKeyFrame KeyTime="0" >
                                                                <DiscreteObjectKeyFrame.Value>
                                                                    <Visibility>Visible</Visibility>
                                                                </DiscreteObjectKeyFrame.Value>
                                                            </DiscreteObjectKeyFrame>
                                                        </ObjectAnimationUsingKeyFrames>
                                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="validationTooltip" Storyboard.TargetProperty="IsOpen">
                                                            <DiscreteObjectKeyFrame KeyTime="0" >
                                                                <DiscreteObjectKeyFrame.Value>
                                                                    <sys:Boolean>True</sys:Boolean>
                                                                </DiscreteObjectKeyFrame.Value>
                                                            </DiscreteObjectKeyFrame>
                                                        </ObjectAnimationUsingKeyFrames>
                                                    </Storyboard>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>

                                        <TextBlock
                                              x:Name="contentPresenter"
                                              Text="{TemplateBinding Content}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}">
                                            <TextBlock.Style>
                                                <Style TargetType="{x:Type TextBlock}">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="TextBlock.TextDecorations" Value="Underline" />
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <DataTemplate x:Key="Message">
                        <StackPanel Background="{StaticResource MessageBackground}" Height="56px" Width="350px" >
                            <StackPanel Margin="10" Orientation="Horizontal" Background="{StaticResource MessageBackground}">
                                <ContentControl Template="{StaticResource CommandIcon}" 
                                                   ToolTip="Command"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Margin="0,4,0,0"
                                                   Width="20"
                                                   Visibility="{Binding IsPublished, Converter={StaticResource BoolToVisibilityCollapseConverterInverted}}" />
                                <ContentControl Template="{StaticResource EventIcon}" 
                                                   ToolTip="Event"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Margin="0,8,0,0"
                                                   Width="24"
                                                   Visibility="{Binding IsPublished, Converter={StaticResource BoolToVisibilityCollapseConverter}}" />
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="14px" FontWeight="Bold" />
                                    <TextBlock Text="{Binding Time}" FontSize="14px" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>

                    <DataTemplate x:Key="UpdatedValuesTemplate">
                        <StackPanel Background="{StaticResource SagaBackground}"  >
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsValueChanged, Converter={StaticResource BoolToVisibilityCollapseConverter}}" Margin="0 6 0 0">
                                <TextBlock Text="{Binding Name}" Width="100px" Foreground="{StaticResource SagaForeground}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text=" =    " Foreground="{StaticResource SagaForeground}"/>
                                <TextBlock Text="{Binding OldValue}" Foreground="{StaticResource SagaForeground}" FontWeight="Bold"/>
                                <TextBlock Text=" ® " FontFamily="Symbol" Foreground="{StaticResource SagaForeground}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding NewValue}" Foreground="{StaticResource SagaForeground}" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsValueNew, Converter={StaticResource BoolToVisibilityCollapseConverter}}" Margin="0 6 0 0">
                                <TextBlock Text="{Binding Label}" Width="100px" Foreground="{StaticResource SagaForeground}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text=" =    " Foreground="{StaticResource SagaForeground}"/>
                                <TextBlock Text="{Binding NewValue}" Foreground="{StaticResource SagaForeground}" FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>

                    <DataTemplate x:Key="AllValuesTemplate">
                        <StackPanel Background="{StaticResource SagaBackground}" >
                            <ContentPresenter ContentTemplate="{StaticResource UpdatedValuesTemplate}" ContentSource="{Binding}" />
                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsValueNotUpdated, Converter={StaticResource BoolToVisibilityCollapseConverter}}" Margin="0 6 0 0" >
                                <TextBlock Text="{Binding Name}" Width="100px" Foreground="{StaticResource SagaForeground}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text=" =    " Foreground="{StaticResource SagaForeground}" />
                                <TextBlock Text="{Binding NewValue}" Foreground="{StaticResource SagaForeground}" FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>

                    <DataTemplate x:Key="PropertiesPanel">
                        <StackPanel Margin="20 10 0 0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Properties: " Foreground="#B3B3B3"/>
                                <RadioButton x:Name="All" Content="All" Style="{StaticResource PropertiesRadioButton}" />
                                <TextBlock Text=" /" Foreground="#B3B3B3" />
                                <RadioButton x:Name="Updated" Content="Updated" IsChecked="True" Style="{StaticResource PropertiesRadioButton}" />
                            </StackPanel>


                            <ItemsControl ItemsSource="{Binding Values}" >
                                <ItemsControl.Style>
                                    <Style TargetType="{x:Type ItemsControl}">
                                        <Setter Property="ItemTemplate" Value="{StaticResource UpdatedValuesTemplate}" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsChecked, ElementName=Updated}" Value="False">
                                                <Setter Property="ItemTemplate" Value="{StaticResource AllValuesTemplate}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>

                    <DataTemplate x:Key="TimeoutMessage">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <StackPanel Background="{StaticResource SagaBackground}" Grid.Column="1" >
                                <StackPanel Margin="10" Background="{StaticResource SagaBackground}" >
                                    <StackPanel Orientation="Horizontal" >
                                        <ContentControl x:Name="TimeoutIcon" Template="{StaticResource SagaTimeoutIcon}" 
                                                    VerticalAlignment="Top"
                                                    Margin="0 4 4 4" />

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Timeout Requested = " Foreground="{StaticResource SagaBlue}" FontSize="16px" FontWeight="Bold" />
                                            <TextBlock Text="{Binding TimeoutFriendly}" Foreground="{StaticResource SagaBlue}" FontSize="16px" FontWeight="Bold" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                            <ContentPresenter x:Name="TimeoutMessage" Grid.Column="2" ContentTemplate="{StaticResource Message}" ContentSource="{Binding}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0 65 0 0"/>
                        </Grid>
                    </DataTemplate>

                    <DataTemplate x:Key="SagaNode">
                        <Grid>
                        <StackPanel SizeChanged="StackPanel_SizeChanged">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter x:Name="InitialMessage" Grid.Column="0" ContentTemplate="{StaticResource Message}" Content="{Binding StartingMessage}" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                <StackPanel x:Name="Middle" Background="{StaticResource SagaBackground}" Grid.Column="1" >
                                    <StackPanel Margin="10 6" Background="{StaticResource SagaBackground}" >
                                        <StackPanel Orientation="Horizontal" Visibility="{Binding IsTimeout, Converter={StaticResource BoolToVisibilityCollapseConverter}}"  >
                                            <ContentControl Template="{StaticResource SagaTimeoutIcon}" 
                                                    VerticalAlignment="Top"
                                                    Margin="0 4 4 4" />

                                            <TextBlock Text="Timeout Invoked" Foreground="{StaticResource SagaBlue}" FontSize="16px" FontWeight="Bold" />
                                        </StackPanel>
                                        <StackPanel Margin="10" Visibility="{Binding IsTimeout, Converter={StaticResource BoolToVisibilityCollapseConverterInverted}}"  >
                                        </StackPanel>

                                        <StackPanel x:Name="StepName" Orientation="Horizontal" >
                                            <ContentControl Template="{StaticResource SagaInitiatedIcon}" 
                                                    VerticalAlignment="Top"
                                                    Margin="0 4 4 4"
                                                    Visibility="{Binding IsFirstNode, Converter={StaticResource BoolToVisibilityCollapseConverter}}" />
                                            <ContentControl Template="{StaticResource SagaUpdatedIcon}" 
                                                    VerticalAlignment="Top"
                                                    Margin="0 4 4 4"
                                                    Visibility="{Binding IsFirstNode, Converter={StaticResource BoolToVisibilityCollapseConverterInverted}}" />

                                            <TextBlock x:Name="StepNameBox" Text="{Binding Label}" FontSize="16px" FontWeight="Bold" />
                                        </StackPanel>

                                        <ContentPresenter ContentTemplate="{StaticResource PropertiesPanel}" ContentSource="{Binding}" />
                                    </StackPanel>
                                </StackPanel>
                                <ItemsControl x:Name="SagaMessages" Grid.Column="2" ItemTemplate="{StaticResource Message}" ItemsSource="{Binding Messages}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0 80 0 0"/>
                            </Grid>
                            <ItemsControl x:Name="TimeoutMessages" ItemTemplate="{StaticResource TimeoutMessage}" ItemsSource="{Binding TimeoutMessages}"/>
                        </StackPanel>
                        </Grid>
                    </DataTemplate>

                    <DataTemplate x:Key="Header">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <StackPanel Background="{StaticResource SagaBackground}" Grid.Column="1" >
                                <StackPanel Margin="10" Background="{StaticResource SagaBackground}" >
                                    <Border BorderThickness="0 0 0 2" Padding="0 5 0 5" BorderBrush="{StaticResource SagaBlue}">
                                        <TextBlock Text="{Binding Name}" FontSize="24px" FontWeight="Bold" />
                                    </Border>
                                    <StackPanel Orientation="Horizontal" Margin="0 5 0 2">
                                        <TextBlock FontWeight="Bold" FontSize="14px" Text="guid " Foreground="{StaticResource SagaForeground}"/>
                                        <TextBlock FontSize="14px" Text="{Binding Guid}" Foreground="{StaticResource SagaForeground}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>

                    <DataTemplate x:Key="Footer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <StackPanel Background="{StaticResource FooterBackground}" Grid.Column="1" >
                                <StackPanel Margin="11" Orientation="Horizontal" >
                                    <ContentControl Template="{StaticResource SagaCompletedIcon}" 
                                                    VerticalAlignment="Top"
                                                    Margin="0 4 4 4" />
                                    <StackPanel >
                                        <TextBlock Foreground="White" Text="Saga Complete" FontSize="14px" FontWeight="Bold"/>
                                        <TextBlock Foreground="White" Text="{Binding CompleteTime}" FontSize="14px" />
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <ScrollViewer>
        <StackPanel>
            <ContentPresenter ContentTemplate="{StaticResource Header}" Content="{Binding}"/>
            <ItemsControl ItemsSource="{Binding Steps}" ItemTemplate="{StaticResource SagaNode}" />
            <ContentPresenter ContentTemplate="{StaticResource Footer}" Content="{Binding}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
